import { ConnectionConfig } from 'mysql';

const options:ConnectionConfig = {
	host: '127.0.0.1',
	port: 3306,
	user: 'root',
	password: 'password',
	database: 'spectrum'
};

export default options;